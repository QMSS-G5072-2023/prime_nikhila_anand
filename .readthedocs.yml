# Required
version: 2

# Image to use
build:
  os: ubuntu-20.04
  tools:
    python: "3.11"

# Configuration
python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
