# Required
version: 2

# Image to use
build:
  os: ubuntu-20.04
  image: testing
  tools:
    python: "3.11"

# Configuration
python:
  version: 3.11
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
